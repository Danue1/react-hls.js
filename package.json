{
  "name": "react-hls.js",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "cleanup": "rm -rf ./dist",
    "build": "babel src --out-dir dist --extensions \".ts\"",
    "local": "yarn cleanup && yarn build && node dist"
  },
  "dependencies": {
    "hls.js": "^1.0.5"
  },
  "devDependencies": {
    "@types/hls.js": "^1.0.0",
    "@types/react": "^17.0.9",
    "prettier": "^2.3.0",
    "typescript": "^4.1.3"
  }
}
